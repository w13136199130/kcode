{
  "name": "kcode",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "packageManager": "pnpm@11.23.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "test": "vitest run",
    "lint": "oxlint packages apps services evals",
    "lint:deps": "depcruise packages apps services evals --config .dependency-cruiser.cjs",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "dependency-cruiser": "^16.6.0",
    "oxlint": "^0.16.0",
    "prettier": "^3.4.0",
    "turbo": "^2.3.0",
    "vitest": "^2.1.8"
  },
  "pnpm": {
    "ignoredBuiltDependencies": [
      "canvas"
    ]
  }
}
