/**
 * 内置工具 + 跨平台 shell 抽象（tools 模块）+ MCP ToolProvider（mcp 模块），§4.4。
 * P1 落地：read/write/edit、grep+glob（捆绑 ripgrep）、bash（超时/后台/会话级 shell 状态）、
 * 图片/视频/网页多模态输入；P3 落地 MCP 进程管理（--ignore-scripts 安装产物）。
 */
export const TOOLS_PHASE = "P1";
